# Phase 4 completion report — 6 September 2026

**Subsequent test-domain update:** the user has now confirmed `http://fleetiq.fwh.is/`. `build/public-preparation/` has been rebuilt as a 40-file, ready-to-upload HTTP test package with 12 canonical/OG/sitemap URLs and crawl-allowed robots. Sender remains unset; SSL is unconfirmed and no HTTPS redirects are added. See `DEPLOYMENT.md` and the latest `VALIDATION.md` entry. The original report below is historical; its unknown-domain/upload-blocked statements are superseded by this update.

Local implementation and Chrome validation pass. The website is prepared for test deployment, but the current preparation package is **not ready to upload** until the actual domain, host and authorised mail sender are configured and a final package is rebuilt. Approved pages were not redesigned. No external email, upload, commit or push was performed.

1. **Files created.** This continuation created `.gitignore`, `DEPLOYMENT.md`, `PHASE4-REPORT.md`, `build/package-manifest.txt` and the 40-file `build/public-preparation/` copy. Earlier Phase 4 additions already present were `privacy.php`, `terms.php`, `404.php`, `assets/css/legal.css`, `includes/bootstrap.php`, and `tools/{build-public.php,config.example.php,public-files.php,router.php}`. QA scripts/results/screenshots are in `%TEMP%/fleetiq-phase4-final-qa`, outside the public package. The pre-existing `README.zip` was left untouched and excluded.
2. **Files modified.** This continuation changed `tools/build-public.php`, `README.md` and `VALIDATION.md`. Existing Phase 4 modifications were preserved in `about.php`, `contact.php`, `assets/css/{site.css,commercial.css}`, `assets/js/site.js`, `components/{header.php,footer.php}`, `includes/{config.php,seo.php}`, `robots.txt` and `sitemap.xml`. The builder now accepts new destinations under the dedicated ignored `build/` directory and supports explicit domain-free preparation; normal builds still require HTTPS and refuse overwrite/source-directory destinations.
3. **Privacy.** Reviewed against the current enquiry implementation: collected fields, response purposes, no automatic marketing, technical processing, necessary contact session cookie, retry-state expiry, retention criteria, security, provider categories, rights/contact route and updates are covered. No invented registration, office, DPO, analytics or marketing systems. Operator identity and actual provider/retention/processing details remain deployment inputs; this is not a legal-completeness certification. Both [ICO public-rights](https://ico.org.uk/for-the-public/) and [complaints](https://ico.org.uk/make-a-complaint/) links resolve. The operator should confirm/document the chosen legitimate-interest basis against [ICO guidance](https://ico.org.uk/for-organisations/uk-gdpr-guidance-and-resources/lawful-basis/a-guide-to-lawful-basis/legitimate-interests/).
4. **Terms.** Website terms of use only. Explicitly exclude subscription, licence, SLA and customer-contract formation. No pricing or response-time commitments or invented corporate details.
5. **404.** Branded, readable Home/Explore actions; direct and unknown nested URLs return 404. Root and configured subdirectory bases resolve correctly. `noindex, follow`, no canonical, excluded from the configured sitemap. Apache/Nginx instructions are documented; actual host enforcement remains to be verified.
6. **Contact/demo.** Recipient remains `iancrofts@live.co.uk`; every Book a Demo targets `contact.php`. Required/optional fields, CSRF, honeypot, email/length/control-character validation, CRLF/header injection defences, escaped retry values, accessible errors and POST/303/GET pass. Success and failure were exercised with loopback SMTP acceptance/rejection; no external mail was sent.
7. **Mail still required.** Supply real `FLEETIQ_DEMO_FROM`, provider-supported `mail()` transport and sender authentication, then authorise/verify a real receipt test. No default sender or credentials were added. Transport acceptance is not proof of inbox receipt.
8. **Base URL/domain.** `FLEETIQ_SITE_URL` remains centrally configurable and unset in source. Local development works without invented canonical/OG URLs. Temporary QA used a reserved HTTPS origin solely to verify all 12 canonicals/OG URLs and sitemap paths, including a subdirectory. Set the actual URL both at build time and runtime; rebuild without `--prepare`.
9. **Public error handling.** An intentionally invalid configuration in an isolated server produced a generic HTTP 500 with no configuration sentinel, exception, warning, stack trace or filesystem path in the response. Form failures remain visible. Startup/entry-point parse errors require host-level display suppression; private logging/session settings and real server rules remain hosting checks.
10. **Navigation.** 326 link references audited; internal page/fragment targets resolve and all six asset references load. No dead `#`, JavaScript placeholder, localhost href or obsolete demo-dialog links. 468 actual anchor activations in browser iframes passed: logo, six Product destinations, main navigation/demo, footer logo/navigation/legal links from all 13 pages at 1440 and 390px. Destination menus were closed. Product is a disclosure; footer Product goes to Features.
11. **SEO.** All 13 routes have unique titles/descriptions, one H1 and logical main heading levels. Open Graph title/description/type metadata is present. Configured canonicals and `og:url` match on all 12 indexable pages; 404 intentionally has neither. Configured sitemap has exactly 12 pages; robots references it. Preparation sitemap is empty and robots disallows crawling pending rebuild. No keyword stuffing was introduced.
12. **Responsive.** Chrome 152, actual PHP-rendered pages: 13 pages × 26 widths = 338 cases. Widths: 1920, 1680, 1441, 1440, 1439, 1366, 1201, 1200, 1199, 1101, 1100, 1099, 1025, 1024, 1023, 769, 768, 767, 601, 600, 599, 481, 480, 479, 390, 320. Zero document overflow, horizontally escaped elements, clipped conceptual-product descendants or overlapping siblings in checked hero/card/benefit/CTA/footer/contact layout groups. Existing intentional handset/dashboard overlay retained. Screenshots sampled for Privacy mobile, Terms desktop, 404 mobile and homepage desktop. Footer is shared consistently. No layout fix was necessary.
13. **Accessibility.** 26 trusted Chrome keyboard cases passed Product Enter/Tab/Escape, visible focus, mobile Menu Enter/Escape and reduced motion. Ten form-error layouts (nine requested widths plus 320px) showed five associated errors, focused the error summary and had no overflow. No duplicate IDs or heading-level skips. These checks are not a full WCAG, screen-reader, browser zoom or real-touch certification. Firefox/Safari and real devices were unavailable. Edge was installed but its attempted headless debugging endpoint did not start, so no Edge pass is claimed.
14. **Claims/content.** Public PHP/JSON reviewed and searched for the prohibited claims. No positive DVSA approval, compliance guarantee, GPS/telematics/route optimisation, AI/predictive maintenance, fake integration/customer/review/testimonial/savings/ROI claims found. Two guarantee matches explicitly disclaim guarantees. Product visuals remain labelled conceptual/illustrative.
15. **Package location.** `C:/Users/iancr/FleetIQ/build/public-preparation/`. Separate copied output; source files retained. `build/` is ignored by Git. Rebuild to a new directory such as `build/public-test/` after configuring the actual URL.
16. **Exact contents/exclusions.** 40 files: 13 root PHP routes; seven `includes` files; eight component PHP files; four CSS files; one JS file; one SVG favicon; `sitemap.xml`, `robots.txt`, root `.htaccess`, `includes/.htaccess`, `components/.htaccess`, `.user.ini`. Every path is listed in `build/package-manifest.txt`. No repository metadata, Codex files, CLI/security/HTTP/browser tests, fixtures, screenshots, development artifacts, archives, docs, secrets or temporary files enter the allowlist. Source-copy hashes matched. The manifest itself remains outside the public folder.
17. **Deployment instructions.** `DEPLOYMENT.md` gives the short upload/configuration/check sequence; README provides private config, Apache/Nginx, PHP settings, HTTPS, session, mail and privacy detail. Code minimum PHP 8.1; deploy on latest patched PHP 8.4 (tested branch), not unsupported 8.1; [PHP support reference](https://www.php.net/supported-versions.php).
18. **Test results.** PHP 8.4.25 lint: all 32 source PHP files pass. Security: 48/48. HTTP/form: 36/36. Isolated SMTP/PRG: 10/10. Package/configuration/error: 52/52. Three builder guards pass (missing domain, existing destination, source destination); copied source hashes match. Responsive/containment: 338/338. Keyboard: 26/26. Actual navigation: 468/468. Form error layouts: 10/10. Internal links/assets, unique metadata/IDs/headings, sitemap/robots and direct/nested 404 checks pass. Browser console contained only the expected 404 document response, no JS exceptions or unexpected asset errors. `git diff --check` passes. Apache/Nginx directives were inspected, not executed by the PHP development router.
19. **Still required before upload/public testing.** Confirm actual host/domain/subdirectory, authorised sender and working mail transport; set private runtime configuration and rebuild a final package. Confirm operator/provider/retention details before public enquiry collection. Verify real-host HTTPS, PHP display suppression, private paths, access rules, custom 404 and mail receipt. Complete unavailable browser/device/assistive-technology coverage. No technical input can be safely invented to close these gaps.
20. **Readiness.** Ready for host configuration and final test-package generation. **Not yet ready for an unconditional live upload or public launch.** Remaining gates are explicitly documented; the domain-free preparation package must not be uploaded as the final build.

Raw evidence: `%TEMP%/fleetiq-phase4-final-qa/{layout,containment,keyboard,metadata,audit,navigation,errors,console}.json`, screenshots and the HTTP/package/SMTP harnesses. Earlier VALIDATION sections are historical and may describe superseded dialogs or environments.
